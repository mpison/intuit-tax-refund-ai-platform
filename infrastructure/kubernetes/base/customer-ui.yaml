apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-ui
  namespace: refund-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-ui
  template:
    metadata:
      labels:
        app: customer-ui
    spec:
      containers:
        - name: customer-ui
          image: customer-ui:0.3.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: customer-ui
  namespace: refund-platform
spec:
  selector:
    app: customer-ui
  ports:
    - name: http
      port: 80
      targetPort: http
