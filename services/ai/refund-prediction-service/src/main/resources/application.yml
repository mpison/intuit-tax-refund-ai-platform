server:
  port: 8070

spring:
  application:
    name: refund-prediction-service

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
  endpoint:
    health:
      probes:
        enabled: true
